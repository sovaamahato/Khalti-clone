import 'package:flutter/material.dart';

class Transactions extends StatelessWidget {
//  ort({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Transactions page")),);;
  }
}