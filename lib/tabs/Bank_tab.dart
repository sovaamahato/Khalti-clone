import 'package:flutter/material.dart';

class BankTab extends StatelessWidget {
  const BankTab({super.key});

  @override
  Widget build(BuildContext context) {
   return Container(child: Center(child: Text("Banks "),),);
  }
}